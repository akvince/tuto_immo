{% extends 'base.html.twig' %}

{% block title 'agence_immo' %}

{% block body %}
    <div class="container">
        <h1>home page</h1>

        <div class="row flex">
            {% for property in properties %}
                {% include 'components/card_property.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}